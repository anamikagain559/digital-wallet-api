// import { Router } from "express";
// import { walletController } from "./wallet.controller";

// const router = Router();

// router.post("/", walletController.createWallet);

// router.get("/me", walletController.getMyWallet);

// router.get("/", walletController.getAllWallets);

// router.post("/:walletId/add-money", walletController.addMoney);

// router.post("/:walletId/withdraw", walletController.withdrawMoney);

// router.post("/:walletId/transfer", walletController.transferMoney);

// router.delete("/:walletId", walletController.deleteWallet);

// export const walletRoutes = router;

